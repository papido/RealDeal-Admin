import React from "react";
import { StyleSheet, Text, View } from "react-native";

const two = () => {
  return (
    <View>
      <Text>two</Text>
    </View>
  );
};

export default two;

const styles = StyleSheet.create({});
